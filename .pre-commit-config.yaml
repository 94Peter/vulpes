repos:
-   repo: local
    hooks:
    -   id: dagger-ci
        name: Dagger CI Checks
        # 呼叫你在 dagger/main.go 定義的專案檢查函式
        entry: dagger call run-all-checks --source=.
        language: system
        pass_filenames: false
        always_run: true